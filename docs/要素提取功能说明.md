# 要素提取功能说明

> **对应需求**：业务需求说明书 — 企业材料上传与信息采集（步骤2：材料解析与提取）
>
> **前置条件**：已完成材料上传与提交（详见 [`上传材料功能说明.md`](上传材料功能说明.md)）
>
> **参考 API**：`api.json`（Postman Collection — dib_api 资料详情接口）

---

## 一、核心业务逻辑

### 1.1 背景

前端提交资料（`POST /techfin/sxd/submit-materials`）成功后，后台系统会自动对上传的文档进行解析和要素提取。由于文档解析是异步过程，前端需要轮询查询提取进度，以决定何时展示提取结果或允许下一步操作。

### 1.2 查询接口

```
GET /techfin/sxd/extract-status/{taskId}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `taskId`（路径变量） | String | 是 | 提交资料返回的任务 ID，格式 `TASK-<32位hex>` |

**成功响应：**

```json
// 全部文档已结束提取（含成功或失败）
{
  "code": 0,
  "msg": "成功",
  "data": {
    "completed": true,
    "pendingDocNames": []
  }
}

// 仍有文档正在提取或等待提取
{
  "code": 0,
  "msg": "成功",
  "data": {
    "completed": false,
    "pendingDocNames": [
      "贵州茅台2025年半年度报告.pdf",
      "商业计划书_v2.pdf"
    ]
  }
}
```

### 1.3 判断逻辑

| 外部 `extractState` | 含义 | 是否计入 `pendingDocNames` |
|---------------------|------|--------------------------|
| `U` | 待执行（Unstarted） | ✅ 是 |
| `I` | 执行中（In Progress） | ✅ 是 |
| `F` | 执行失败（Failed） | ❌ 否（视为已结束） |
| `Y` | 执行完成（Yes） | ❌ 否（视为已结束） |

- **全部完成**：遍历 `sxd_doc` 表中该 `taskId` 关联的所有文档，逐个调用外部资料详情接口查询 `extractState`。若所有文档的状态均为 `F` 或 `Y`，则判定为完成，返回 `completed: true`。
- **待处理**：只要存在任一文档的状态为 `U` 或 `I`，则返回 `completed: false`，并将这些文档的名称（`docName`）放入 `pendingDocNames` 列表供前端展示。
- **边界情况**：如果 `taskId` 在 `sxd_doc` 表中无关联记录，抛出 `DOC_NOT_FOUND` 异常。

### 1.4 提取数据查询

当前端轮询到提取状态 `completed=true` 后，调用此接口获取商业计划书各部分的提取文本。

```
GET /techfin/sxd/extract-data/business/{taskId}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `taskId`（路径变量） | String | 是 | 提交资料返回的任务 ID |

**成功响应：**

```json
{
  “code”: 0,
  “msg”: “成功”,
  “data”: [
    { “tableName”: “dib_manage_company_profile”, “text”: “深圳市WG药业有限公司（简称”WG药业”）成立于2015年……” },
    { “tableName”: “dib_director_keyresume”, “text”: “王美女士，深圳大学商务管理专业……\n李鹏博士，17年高端药物制剂研发经历……” },
    { “tableName”: “dib_manage_business_and_products”, “text”: “深圳市WG药业有限公司定位为专注儿童处方药……” },
    { “tableName”: “dib_manage_business_circumstance”, “text”: “深圳市WG药业有限公司定位为专注儿童处方药的企业……” },
    { “tableName”: “dib_company_qualification”, “text”: “公司荣获国家高新技术企业……” },
    { “tableName”: “dib_manage_progressiveness_description”, “text”: “WG药业构建了”自研创新+合作引进”双轮驱动……” },
    { “tableName”: “dib_manage_competitive_advantages”, “text”: “首先，公司构建了顶尖的全球化管理团队……” },
    { “tableName”: “dib_manage_development_strategy”, “text”: “深圳市WG药业有限公司确立了……” },
    { “tableName”: “dib_manage_y_industry_analysis”, “text”: “中国儿童用药市场呈现出增速快、潜力大……” }
  ]
}
```

#### 判断逻辑

1. 根据 `taskId` 从 `sxd_doc` 表中查询 `businessType = 1455619597903695872`（商业计划书）的文档列表。
2. 对每个文档，遍历预设的 9 个 `tableName`，逐个调用外部提取数据查询 API：
   - `dib_manage_company_profile` — 公司介绍
   - `dib_director_keyresume` — 实际控制人及团队简介
   - `dib_manage_business_and_products` — 主营业务和产品
   - `dib_manage_business_circumstance` — 经营情况介绍
   - `dib_company_qualification` — 资质认证与荣誉
   - `dib_manage_progressiveness_description` — 研发成果与转化情况
   - `dib_manage_competitive_advantages` — 竞争优势介绍
   - `dib_manage_development_strategy` — 发展战略情况
   - `dib_manage_y_industry_analysis` — 行业发展情况
3. 每个 `tableName` 对应外部 API 响应中不同的文本字段（见下方字段映射表），后端提取后按 `tableName` 聚合。
4. 对于 `dib_director_keyresume`，该表可能返回多个人员记录，将各记录的 `resume` 字段以换行符连接合并为一个 `text`。
5. 将提取结果写入 `sxd_extract_data` 表缓存（按 `task_id` + `doc_id` + `table_name` 唯一标识），后续接口直接从缓存表读取，不再重复调用外部 API。
6. 按固定顺序返回所有 9 个 `tableName` 的 `text`，即使某表提取结果为空也保留条目（`text` 为空字符串）。

#### 字段映射规则

| tableName | 外部响应 data 中的字段 | 说明 |
|-----------|----------------------|------|
| `dib_manage_company_profile` | `companyProfileText` | 公司介绍全文 |
| `dib_director_keyresume` | `resume`、`position` | 多条记录，格式：职位：position，简介：resume，各记录间换行合并 |
| `dib_manage_business_and_products` | `businessAndProductsText` | 主营业务和产品介绍 |
| `dib_manage_business_circumstance` | `text` | 经营情况描述 |
| `dib_company_qualification` | `text` | 资质认证描述 |
| `dib_manage_progressiveness_description` | `progressivenessText` | 研发成果描述 |
| `dib_manage_competitive_advantages` | `competitivenessText` | 竞争优势描述 |
| `dib_manage_development_strategy` | `strategyText` | 发展战略描述 |
| `dib_manage_y_industry_analysis` | `text` | 行业发展分析 |

> 外部 API 的 `data` 返回 JSON 数组，数组中每条记录包含上述字段之一。后端根据 `tableName` 取对应字段的值，多条记录时按换行合并。

### 1.5 提取数据导出

前端提供两个下载按钮，分别导出商业计划书和财务报表的提取结果（xlsx 格式）。

#### 商业计划书导出

```
GET /techfin/sxd/export-data/business/{taskId}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `taskId`（路径变量） | String | 是 | 提交资料返回的任务 ID |

**响应**：商业计划书提取数据的 `.xlsx` 文件（`Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`）

> 商业计划书仅单个文档，后端直接调用外部导出接口后返回文件流，无需压缩。

#### 财务报表导出

```
GET /techfin/sxd/export-data/finance/{taskId}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `taskId`（路径变量） | String | 是 | 提交资料返回的任务 ID |

**响应**：包含多个 xlsx 文件的 `.zip` 压缩包（`Content-Type: application/zip`），内部文件命名格式为 `财务报表_{reportDate}.xlsx`（如 `财务报表_2024-12-31.xlsx`），同一日期有多份文件时自动追加序号（如 `财务报表_2024-12-31_2.xlsx`）。

> 财务报表涉及多份文件，后端逐个调用外部导出接口下载后，将所有 xlsx 打包为 zip 压缩包返回。

#### 导出判断逻辑

1. 根据 `taskId` + `businessType` 查询 `sxd_doc` 表获取对应的 `docId` 列表
   - 商业计划书：`businessType = 1455619597903695872`
   - 财务报表：`businessType = 1273757631042949120`
2. 调用外部导出资料接口 `GET /api/extract/open/doc/exportDocData/{docId}`，接口返回 xlsx 文件流
3. 商业计划书：直接以 xlsx 格式返回给前端
4. 财务报表：遍历每个 `docId` 下载 xlsx，将所有文件打包为 zip 后返回

---

## 二、处理流程

```
① 前端轮询 → GET /techfin/sxd/extract-status/{taskId}

② 后端处理 → 根据 taskId 从 sxd_doc 表查询所有 docId
         → 遍历每个 docId，调用外部资料详情 API
         → 聚合所有文档的 extractState

③ 结果判定 → 全部为 F 或 Y → completed: true
         → 存在 U 或 I   → completed: false + pendingDocNames

④ 提取数据查询（completed=true 后调用）：
         → 后端先查 sxd_extract_data 缓存表
         → 缓存命中 → 直接返回 {tableName, text} 列表
         → 缓存未命中 → 查 sxd_doc 获取 docId → 遍历 9 个 tableName 调外部 API
         → 提取结果写入 sxd_extract_data 缓存，后续请求直接走缓存

⑤ 前端展示：
     completed=true  → 调用 GET /sxd/extract-data/business/{taskId} 获取提取结果并展示
     completed=false → 显示加载动画 + 列出待处理文档名称

⑥ 提取数据导出：
         → 前端根据类型调用对应导出接口
         → 商业计划书：GET /sxd/export-data/business/{taskId} → 单 xlsx 文件
         → 财务报表：   GET /sxd/export-data/finance/{taskId}  → zip 压缩包
         → 后端分别查 sxd_doc 获取 docId → 调外部导出接口 → 下载文件流返回
```

---

## 三、外部 API 调用

### 资料详情 `GET /api/extract/open/doc/get/{docId}`

```
Headers:
  c1-token: <从 application.properties 配置读取>

Response 200:
{
  "success": true,
  "code": "1",
  "message": "操作成功",
  "data": {
    "id": "1522204279569448960",
    "docName": "3.贵州茅台2025年半年度报告（公开信息）.pdf",
    "projectId": "1521841328915996672",
    "attId": "1522204270320672768",
    "docTypeId": "1273757631042949120",
    "extractState": "Y",
    "classifyState": "20",
    "convertState": "Y",
    "verifyState": "N",
    "auditState": "Y",
    "machineCompletionRate": 100.0,
    "completionRate": 100.0
  }
}
```

> 本功能只关注 `extractState` 字段，其余状态字段（`classifyState`、`convertState` 等）由外部系统内部流转。

### 调用方式

- **HTTP 方法**：`GET`
- **鉴权**：请求头 `c1-token`，值统一在 `api.default-token` 配置
- **URL 拼接**：`api.doc-detail-url/{docId}`（配置中的 `api.doc-detail-url` 不含末尾的 `/{docId}`）
- **响应解析**：统一使用 `ExternalResponse` 包装，通过 `getDataAs(DocDetailData.class)` 转换 data 字段

---

### 提取数据查询 `POST /api/extract/open/doc/queryData`

```
Headers:
  c1-token: <从 application.properties 配置读取>
  Content-Type: application/json

Request Body:
{
  "docId": 1522659418314964992,
  "tableName": "dib_manage_company_profile"
}
```

**Response 200（dib_manage_company_profile）：**
```json
{
  “data”: [
    {
      “company_profile_text”: “深圳市WG药业有限公司（简称”WG药业”）成立于2015年……”
    }
  ],
  “success”: true,
  “code”: “1”,
  “message”: “操作成功”
}
```

**Response 200（dib_director_keyresume）：**
```json
{
  "data": [
    {
      "resume": "王美女士，深圳大学商务管理专业……",
      "name": "王美",
      "position": "创始人、董事长、总经理"
    },
    {
      "resume": "李鹏博士，深圳市高层次人才……",
      "name": "李鹏",
      "position": "首席技术官、副总经理"
    }
  ],
  "success": true,
  "code": "1",
  "message": "操作成功"
}
```

> `dib_director_keyresume` 返回多条人员记录，后端将每条记录格式化为"职位：position，简介：resume"，各记录间用换行合并为一个 `text`。
>
> 响应 `data` 为 JSON 数组，每项包含一个文本字段（字段名因 `tableName` 而异，详见 [字段映射规则](#字段映射规则)）。

**请求参数**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `docId` | Long | 是 | 文档 ID，来自 `sxd_doc.doc_id`，类型为数值 |
| `tableName` | String | 是 | 表名，对应商业计划书提取的不同部分 |

**调用方式**

- **HTTP 方法**：`POST`，请求体为 JSON
- **鉴权**：请求头 `c1-token`，值统一在 `api.default-token` 配置
- **URL**：`api.doc-query-data-url`（完整地址，不含路径变量）
- **请求体序列化**：全局 Jackson 默认 camelCase，请求体字段 `docId`、`tableName` 即为 camelCase，无需额外标注
- **响应解析**：响应 `data` 数组元素字段为 snake_case（如 `company_profile_text`、`current_amount`），对应的 DTO（`BpExtractRecord`、`FinanceRecord`、`AuditReportItem`）显式标注 `@JsonNaming(SnakeCaseStrategy.class)` 以匹配；使用 `ExternalResponse` 包装，通过 `getDataAsList(...)` 转换 data 数组

---

### 导出资料数据 `GET /api/extract/open/doc/exportDocData/{docId}`

```
Headers:
  c1-token: <从 application.properties 配置读取>

Response: 文件二进制流（Content-Type: application/octet-stream）
```

> 该接口直接返回文件二进制流，非 JSON 格式。后端使用 `RestTemplate` 以 `byte[]` 类型接收，不经过 `ExternalResponse` 包装。

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `docId`（路径变量） | Long | 是 | 文档 ID，来自 `sxd_doc.doc_id` |

**调用方式**

- **HTTP 方法**：`GET`
- **鉴权**：请求头 `c1-token`，值统一在 `api.default-token` 配置
- **URL 拼接**：`api.doc-export-data-url/{docId}`（配置中的 `api.doc-export-data-url` 不含末尾的 `/{docId}`）
- **响应处理**：后端收到文件流后，区分两种场景：
  - 商业计划书（单个文件）：直接以 xlsx 格式返回给前端
  - 财务报表（多个文件）：逐个下载后打包为 zip，以 zip 格式返回给前端

---

## 四、数据模型

### sxd_doc 表（文档明细表）

查询提取状态时，通过 `task_id` 索引获取该任务下的所有 `docId`。

| 字段 | 类型 | 说明 |
|------|------|------|
| `doc_id` | VARCHAR(64) (PK) | 资料批量新增返回的文档 ID |
| `task_id` | VARCHAR(64) | 关联 `sxd_record.task_id`（已建索引 `idx_task_id`） |
| `business_type` | VARCHAR(32) | 业务类型（docTypeId） |
| `report_date` | VARCHAR(10) | 财报报告日期 |

### sxd_extract_data 表（提取数据缓存表）

提取数据查询时，将外部 API 返回的提取文本写入此表缓存，后续接口直接从缓存读取，避免重复调用外部 API。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT (PK, AUTO_INCREMENT) | 自增主键 |
| `task_id` | VARCHAR(64) | 关联 `sxd_record.task_id` |
| `doc_id` | VARCHAR(64) | 关联 `sxd_doc.doc_id` |
| `table_name` | VARCHAR(128) | 提取表名，如 `dib_manage_company_profile` |
| `text` | TEXT | 提取文本内容 |
| `created_at` | DATETIME | 创建时间 |
| `updated_at` | DATETIME | 更新时间 |

> **数据生命周期**：
> - **写入**：`GET /sxd/extract-data/business/{taskId}` 首次调用时，从外部 API 拉取数据后写入此表。
> - **读取**：`GET /sxd/extract-data/business/{taskId}` 后续调用直接读缓存；`POST /sxd/report` 报告生成时读取。
> - **清理**：报告生成完成后删除该 `task_id` 的记录；定时任务每天凌晨 2:00 清理 `sxd_record` 中状态为 `"0"`（未完成）的 `task_id` 对应记录。

### 关键 DTO

| 类 | 位置 | 说明 |
|----|------|------|
| `ExtractStatusResponse` | `model/.../dto/response/` | 提取状态查询响应体：`completed` + `pendingDocNames` |
| `ExtractDataItem` | `model/.../dto/response/` | 提取数据项：`tableName` + `text` |
| `DocDetailData` | `model/.../dto/external/` | 外部资料详情 API 的 data 映射：`docName`, `extractState` |
| `BpExtractRecord` | `model/.../dto/external/` | 商业计划书提取数据查询 API 的 data 数组元素，汇总所有可能文本字段 |
| `BpExtractRequest` | `model/.../dto/external/` | 外部提取数据查询 API 的请求体：`docId` + `tableName` |
| `ExtractData` | `model/.../entity/` | `sxd_extract_data` 缓存表实体 |

### Service 分层

| 接口 | 实现 | 职责 |
|------|------|------|
| `SxdService` | `SxdServiceImpl` | 材料上传、提交、附件管理、实控人确认、提取状态轮询 |
| `ExtractDataService` | `ExtractDataServiceImpl` | 提取数据查询+缓存、xlsx/zip 导出、Word 报告生成 |

---

## 五、配置项

```properties
# 外部资料详情接口地址（不含 docId，调用时拼接）
api.doc-detail-url=${api.base-url}/api/extract/open/doc/get
# 外部提取数据查询接口地址（POST，完整路径）
api.doc-query-data-url=${api.base-url}/api/extract/open/doc/queryData
# 外部导出资料接口地址（不含 docId，调用时拼接）
api.doc-export-data-url=${api.base-url}/api/extract/open/doc/exportDocData
```

| 属性 | 说明 |
|------|------|
| `api.doc-detail-url` | 资料详情接口基地址，调用时在末尾拼接 `/{docId}` |
| `api.doc-query-data-url` | 提取数据查询接口完整地址（POST JSON） |
| `api.doc-export-data-url` | 导出资料接口基地址，调用时在末尾拼接 `/{docId}` |

---

## 六、错误码

| 错误码 | 触发条件 | HTTP 状态码 |
|--------|---------|------------|
| `DOC_NOT_FOUND` | `taskId` 在 `sxd_doc` 表中无关联记录 | 400 |
| `DOC_DETAIL_FAILED` | 外部资料详情接口调用失败（网络异常 / 非成功响应） | 400 |
| `DOC_QUERY_FAILED` | 外部提取数据查询接口调用失败（网络异常 / 非成功响应 / docId 格式无效） | 400 |
| `DOC_EXPORT_FAILED` | 外部导出资料接口调用失败或返回数据为空（网络异常 / 非成功响应） | 400 |
| `ZIP_CREATE_FAILED` | 财务报表多个 xlsx 打包 zip 时发生 IO 异常 | 400 |

---

## 七、与上传材料功能的关系

```
上传材料功能                         要素提取功能
─────────────────                  ────────────────
POST /upload-attachment     →      （前置步骤）
POST /submit-materials      →      生成 taskId
                                  GET /extract-status/{taskId}
                                  （轮询直到 completed=true）
                                  GET /extract-data/business/{taskId}
                                  （completed=true 后获取提取结果）
                                  GET /export-data/business/{taskId}
                                  （下载商业计划书 xlsx）
                                  GET /export-data/finance/{taskId}
                                  （下载财务报表 zip）
```

> 提取状态查询、提取数据查询和提取数据导出均为上传材料的后续流程，均共享 `sxd_doc` 表作为数据桥梁。提取数据仅在 `completed=true` 后可用，此后前端可进入下一阶段（如实控人确认、[报告生成](报告生成功能说明.md)等）或下载提取结果文件。
>
> **报告生成**：详见 [`报告生成功能说明.md`](报告生成功能说明.md)，该功能在要素提取完成后调用，从结构化提取数据中聚合资产负债表关键科目表，以 markdown 格式返回。
